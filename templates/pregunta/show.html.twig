{% extends 'base.html.twig' %}

{% block title %}Pregunta{% endblock %}

{% block body %}
    <h1>Pregunta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ preguntum.id }}</td>
            </tr>
            <tr>
                <th>Enunciado</th>
                <td>{{ preguntum.enunciado }}</td>
            </tr>
            <tr>
                <th>FechaInicio</th>
                <td>{{ preguntum.fechaInicio ? preguntum.fechaInicio|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FechaFin</th>
                <td>{{ preguntum.fechaFin ? preguntum.fechaFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pregunta_index') }}">back to list</a>

    <a href="{{ path('app_pregunta_edit', {'id': preguntum.id}) }}">edit</a>

    {{ include('pregunta/_delete_form.html.twig') }}
{% endblock %}
